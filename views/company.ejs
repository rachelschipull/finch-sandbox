<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <title>Finch API Project</title>
</head>
<body>

    <!-- Navbar Component -->
    <nav class="navbar bg-body-tertiary bg-primary-subtle">
        <div class="container-fluid">
            <span class="navbar-brand mb-0 h1">API SANDBOX!</span>
            <a class="navbar-brand h1" href="/logout">Logout</a>
        </div>
    </nav>


    <div class="container mt-3 my-3">
    <h1>Finch API Data</h1>

    <!-- Company Data here -->
    <div class="container mx-3">
        <h2>Company Data: <em>/employer/company</em></h2>
        <p>Legal Name: <%= companyData.legal_name %></p>
        <ul>
            <li><span>EIN</span> <%= companyData.ein%> </li>
            <li><span>Entity Type:</span> <%= companyData.entity.type %></li>
            <li><span>Primary Email:</span> <%= companyData.primary_email %></li>
            <li><span>Primary Phone:</span> <%= companyData.primary_phone_number %></li>
            <li><span>Office Location:</span> <%= companyData.locations[0].line1 %> <%= companyData.locations[0].city%> <%= companyData.locations[0].state %> <%= companyData.locations[0].postal_code%> </li>
        </ul>
    </div>

    <!-- Employee select form -->
    <div class="container">
        <form action="/submit" method="POST">
            <div class="mb-3">

                <h2>Select an employee to display their employment information.</h2>
                
                <select class="form-select" aria-label="Default select example" name="employeeId">
                    <option selected>Choose an employee</option>
                    <% for( let i=0; i < directoryData.individuals.length; i++) { %>
                    <option value="<%= directoryData.individuals[i].id %>"> <%= directoryData.individuals[i].first_name %> <%= directoryData.individuals[i].last_name %> </option>
                    <% } %>
                </select>
                
                <button type="submit" class="btn btn-primary my-3">Submit</button>
            </div>
        </form>
    </div>

</div> 
</body>
</html>